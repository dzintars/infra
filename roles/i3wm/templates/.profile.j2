# Set the positions of screens. Run `xrandr` on the target system to see the correct names of the displays for particular GPU drivers.
xrandr --output DVI-I-1 --mode 1920x1080 --rate 60.0 --right-of DP-1-8 --mode 2560x1080 --rate 60.0

# Run Feh background setter
~/.fehbg &

# Load Siji fonts for the Polybar to avoid journalctl warnings
xset +fp /home/dzintars/.fonts
xset fp rehash

#export PATH=$PATH:/usr/local/go/bin
#export GOPATH=$HOME/go

#export PATH="$PATH:/opt/yarn-v1.23.0-20200205.1242/bin"
#export PATH="$PATH:`yarn global bin`"

#export PATH="$HOME/.cargo/bin:$PATH"