---
- name: configure workstation
  hosts: WORKSTATION
  connection: local
  gather_facts: yes
  # become: yes
  # become_user: dzintars
  # become_method: sudo

  roles:
    - role: deploy_bastion
      tags: [ bastion ]
    - role: network
      tags: [ network ]
    - role: openshift
      tags: [openshift]
    - role: libvirt
      tags: [libvirt]
      # become: true
    - role: protoc
      tags: [protoc]
      become: true